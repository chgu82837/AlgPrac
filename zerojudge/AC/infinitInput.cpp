#include <stdio.h>

int main(){
    int y; // variables here
    while(scanf(" %d",&y) != EOF){ // recieve the input or exit
        printf("%d!",y);
        // Do something ...
    }
    return 0;
}
